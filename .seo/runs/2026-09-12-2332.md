# Run 2026-09-12-2332 — Phase 0: JSON-LD schema + internal-link repair

Run id: 2026-09-12-2332 · lane: technical (Phase 0, roadmap item 4) · branch: `seo/phase-0-schema` · git.mode: `pr`

## Measured

First-ever health fingerprint (`health/2026-09-12.json`, 7 pages, live site): **21 violations**.

- `missing-schema` ×7 — every page lacked JSON-LD (the planned Phase 0 item).
- `redirecting-internal-link` ×7 + `orphan-page` ×6 — one root cause: internal links used `.html` hrefs (`work/<slug>.html`, `../index.html`), which 301 to the canonical clean URLs, leaving the clean URLs with no crawlable inbound links.
- `broken-internal-link` ×1 — **false positive**: Cloudflare's edge email-obfuscation rewrites the plain `mailto:` in source into `/cdn-cgi/l/email-protection`, which 404s. Documented, not "fixed" in markup; dashboard toggle is a human call (NY-7).

No GSC, so no rankings panels. Date audit found 5 of 7 sitemap `lastmod` values stale vs git content history (last commit touching visible copy/imagery; delivery-plumbing commits like the 2026-09-08 AVIF/woff2 pass excluded).

## Candidates

Technical lane only: (a) ship the Phase 0 schema, (b) repair the internal-link root cause surfaced by the fingerprint, (c) repair sitemap lastmod. All three are the same small surface (7 hand-written pages), so they ship together.

## Chose

All three — one PR. Schema content pulled verbatim from visible page text (archive header "NAME · descriptor" as Article headline, meta description as description) per the truth contract; dates from git history (datePublished = first commit, dateModified = last content commit).

## Did

- `index.html` — JSON-LD `@graph`: full `Person` (`#person`: name, jobTitle, email, Minneapolis address, LinkedIn sameAs, knowsAbout) + `WebSite` (`#website`). Repointed 6 case-study links to `/work/<slug>`.
- `work/*.html` (all six) — JSON-LD `@graph`: compact `Person` + `Article` (`#article`, mainEntityOfPage, isPartOf → `#website`, headline, description, OG image, dates, author/publisher → `#person`) + `BreadcrumbList` (Index → case study). Repointed the 2 home links to `/` and the next-project link to its clean URL on every page.
- `sitemap.xml` — 5 `lastmod` corrected to content dates: `/` → 2026-09-05, everag → 2026-09-04, conservis → 2026-09-03, vidscrip → 2026-09-03, plinth → 2026-09-03.
- Register: ledger Shipped dates re-derived to match (4 rows fixed) + repair note; roadmap item 4 struck, tracker → in_progress, schema convention rewritten; config `cache.health_fingerprint.read` = 2026-09-12; NY-7 opened.

Gates: all 7 JSON-LD blocks parse as valid JSON with required properties (script check). Post-fix fingerprint against the local tree with `--expect-schema "*"` and link check: **0 violations, 0 redirecting links** (`health/2026-09-12-post.json`). Meta floor untouched — no title/description/canonical changed. No cloaking: every schema value is visible on the page.

## Not done

- Roadmap item 5: sitemap submission in GSC (blocked on NY-1) and Bing (NY-3).
- Rich Results Test on the live URLs — meaningful only after deploy; run post-merge.
- Live re-fingerprint post-merge: expect 0 violations except the documented CF email-obfuscation false positive (NY-7).
- Phase 0 therefore stays `in_progress` until item 5 lands.

## Needs you

Opened: NY-7 (Cloudflare email obfuscation: keep for spam protection vs turn off for clean audits). Still open: NY-1, NY-3, NY-4, NY-6.

## Spend

None. Zero paid calls.

## Hand-off

Commit + PR per `git.mode: pr` — PR #18 (https://github.com/panoptican/spidleweb/pull/18), open and unmerged. Work happened in a git worktree (`…/T/opencode/spidleweb-seo-schema`) because the main tree carries the user's unrelated WIP — those files were never staged or touched.
